{
  "name": "eti-ts-nf",
  "version": "1.0.0",
  "main": "src/main.ts",
  "license": "MIT",
  "dependencies": {
    "typescript": "^5.0.4"
  },
  "devDependencies": {
    "@types/node": "^18.15.11",
    "rimraf": "^5.0.0",
    "ts-node-dev": "^2.0.0"
  },
  "scripts": {
    "dev": "ts-node-dev --transpile-only src/main.ts",
    "build": "rimraf ./build && npx tsc"
  }
}
